# yaml ??? properties?? ?? ??? ??
spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/stock_example
    username: root
    password: dydcjf12!

# JPA?? ??? ??? ???? ???? ??? ??
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
